fastapi==0.110.0 # Updated to latest stable as per requirements
uvicorn==0.28.0 # Updated to latest stable as per requirements
python-dotenv==1.0.1 # For loading environment variables
psycopg2-binary==2.9.9 # PostgreSQL adapter
sqlalchemy==2.0.29 # ORM (if used, otherwise can be removed)
asyncpg==0.29.0 # Async PostgreSQL driver
pydantic==2.7.1 # Data validation and settings management
pydantic-settings==2.2.1 # For managing FastAPI settings from environment
python-jose==3.3.0 # For JWT authentication
passlib==1.7.4 # For password hashing
python-multipart==0.0.9 # For handling form data
httpx==0.27.0 # For making HTTP requests (e.g., to external APIs)
redis==5.0.3 # For Redis client
minio==7.2.20 # For MinIO S3 compatible storage client